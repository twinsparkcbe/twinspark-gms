/**
 * billing module — business logic lives here (called by Server Actions /
 * app/api route handlers, never straight from components).
 *
 * Per project workflow: implementation starts only after the billing feature
 * & use-case list and test cases are confirmed. Nothing exported yet.
 */
export {};
