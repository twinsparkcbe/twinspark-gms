/**
 * Shared GST + discount calculation, reused across Sales, Service, and
 * Billing invoices. GST and discount are optional on all invoice types —
 * keep that toggle here so every invoice path behaves identically.
 *
 * Pending: Billing module feature list + test cases confirmation.
 */
export {};
